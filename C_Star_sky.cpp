#include <bits/stdc++.h>
using namespace std;

#define ll long long

int solve() {
    
    
}

int main() {
    ios_base::sync_with_stdio(0), cin.tie(0), cout.tie(0);
    cout.setf(ios::fixed);

    int T = 1;
    cin >> T;
    while (T--) {
        cout << solve();
    }
    return 0;
}