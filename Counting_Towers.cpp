#include <bits/stdc++.h>
using namespace std;

#define ln '\n'
#define ll long long


int solve(int n) {
    
    
}

int main() {
    ios_base::sync_with_stdio(0), cin.tie(0), cout.tie(0);

    int T = 1;
    cin >> T;
    while (T--) {
        int n;
        cin >> n;
        cout << solve(n) << ln;
    }
    return 0;
}