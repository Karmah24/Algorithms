#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    int palindrome_prefix(string str) {
        
        
    }
};

int main() {
    ios_base::sync_with_stdio(0), cin.tie(0), cout.tie(0);
    freopen("../input.txt", "r", stdin);
    freopen("../output.txt", "w", stdout);

    string str;
    cin >> str;    
    Solution obj;
    cout << obj.palindrome_prefix(str);
}